# 変更履歴

katsu-magi の変更をここに記録します。新しい版が上です。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に倣い、版の付け方は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## 0.3.0 - 2026-09-25

### 変更

- **Enter で送信できるようになりました**。改行は Shift+Enter または Alt+Enter です。Ctrl+Enter でも送信できます(これまでどおり)
- **履歴から選んだ会話に、そのまま続きを書けるようになりました**。これまでは「この会話を再開する」を押すまで入力できませんでした
  - 選んだ時点では表示だけなので、各サイトのタブは動かず、すぐ読めます
  - そのまま送信すると、送る直前に 3 サイトのタブがその会話に戻り、続きとして質問されます
  - 送信せずにタブだけ戻したいときは、帯の「この会話を再開する」が使えます
- 「New conversation」の確認ダイアログを廃止しました。会話は履歴に残るので、押しても失われるものはありません

### 追加

- 入力欄の右上に **「Enter で改行」** のチェックボックスを追加しました。入れると Enter が改行になり、送信は Ctrl+Enter になります。状態は次回も維持されます

### 修正

- **履歴から会話を開いたとき、最後のやり取りが見える位置で表示される**ようになりました。これまでは先頭のまま止まっていて、毎回いちばん下までスクロールする必要がありました
- 現在の会話を履歴から開くと入力できなくなっていた問題を直しました。「現在の会話に戻る」を押したときや、サイドバーで表示中の会話をクリックしたときに、「過去の会話を表示しています」の帯が出たまま送信できない状態になっていました
- 日本語入力の変換を Enter で確定したときに送信されないようにしました
- 開発用サーバー(`pnpm dev`)で画面の操作がサーバーに届かない問題を直しました。配布版(`pnpm start`)には影響しません

## 0.2.0 - 2026-09-24

### 修正

- **回答が表示されないことがある問題を直しました(特に Gemini)**。原因は二つありました。
  - 回答の入れ物ができてから中身が入るまでの間を、失敗と誤判定していました。Gemini は入れ物の表示に 8 秒ほどかかり、簡単な質問でも完了まで 30 秒を超えることがあります。入れ物ができた時点で送信は通っているので、以後は初回の待ち時間の上限を適用しないようにしました
  - 会話が長くなると回答を見失っていました。Gemini は画面の外に出た古い回答を消すため、「何番目の回答か」で数える方法ではずれていきます。送信の直前に既存の回答へ印を付け、印の無いものを読む方法に変えました
- 回答の文字が取れないことがある場合に備え、別の経路でも取得するようにしました。停止ボタンの見つけ方も堅くしました

### 追加

- **会話履歴**。やり取りが自動で保存されます。操作は要りません
  - 左のパネルに過去の会話が日付ごとに並びます。「◀ 閉じる」で畳め、開閉状態は次回も維持されます
  - 題名をクリックすると内容を**参照**できます。各サイトのタブは動きません
  - **「再開」**を押すと 3 サイトのタブが当時の会話に戻り、続きから追い質問できます
  - 題名はダブルクリックで変更できます。「削除」は katsu-magi の履歴から消すだけで、各サイト側の会話は残ります
  - 保存先は `%LOCALAPPDATA%\katsu-magi\history\` です
- **起動時の自動再開**。サーバーを終了しても、次に起動すると最後の会話から続けられます。設定 `history.resumeLastOnStart` を `false` にすると常に新しい会話で始まります
- 配布 zip にこの変更履歴を同梱するようにしました
- 不具合の切り分け用に `adapter:test` へ `--debug` を追加しました。送信のたびの観測値(入れ物はできたか、サイトはまだ生成中と言っているか)が出ます

### 変更

- 待ち時間の既定値を延ばしました。回答が現れるまでの上限を 45 秒から 120 秒へ、生成全体の上限を 240 秒から 420 秒へ
- 設定に `history.enabled` と `history.resumeLastOnStart` を追加しました
- Chrome の起動時に、背面のタブの動作が遅くならないようにする指定を加えました

## 0.1.0 - 2026-09-21

最初の版です。

### 追加

- **3 サイトへの同時送信**。一つのプロンプトを ChatGPT、Gemini、Claude の正規サイトへ同時に送り、回答を横並びで表示します。ログイン済みの本物の Chrome を操作するので、追加料金はかかりません
- **会話の継続**。追い質問は各サイトの同じ会話に送られます。「New conversation」で 3 サイトを一括で新しい会話にします
- **Markdown 整形**。回答は生成中から流れ、完了すると見出し、箇条書き、表、コードブロックが整った形になります
- **状態表示と復帰**。ログイン要求、確認画面、利用上限、画面構造の変化を検知してバッジで示し、対処後に「Retry」で戻れます
- **表示の調整**。ブラウザの横幅いっぱいに表示し、カラムの間の仕切りをドラッグして幅を変えられます。幅は次回も維持されます。回答内のリンクは別タブで開きます
- **保守用コマンド**。ログイン、1 サイトだけの動作確認、セレクタの点検と調査
- **ポータブル配布**。`pnpm package` で Node.js を同梱した zip を作れます。受け取る側に必要なのは Windows と Chrome だけです
